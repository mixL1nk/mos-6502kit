[package]
name = "cpu"
version = "0.1.0"
edition = "2024"

[lib]
doctest = false

[dependencies]
bitflags = "2.9.0"
derive_more = { workspace = true }
common = { path = "../common" }
memory = { path = "../memory" }
error = { path = "../error" }
types = { path = "../types" }
